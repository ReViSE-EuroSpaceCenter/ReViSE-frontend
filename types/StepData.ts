import {StepBonus} from "@/types/StepBonus";

export type StepData = {
    id: number;
    title: string;
    text: string;
    4: {
        bonuses: StepBonus[];
        resources: string[];
    },
    6: {
        bonuses: StepBonus[];
        resources: string[];
    }
};